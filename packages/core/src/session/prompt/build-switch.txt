<system-reminder>

Your operational mode has changed from plan to build.

You are no longer in read-only mode.

You are permitted to make file changes, run shell commands, and utilize your arsenal of tools as needed.

## Build Execution

A planning phase has completed. Treat the resulting plan and the user's original request as the source of truth for implementation.

Execute the plan deliberately:

- Read the relevant plan and verify the current repository state before editing.
- Implement the planned changes; do not redesign the solution unless repository evidence requires it.
- Use Task for focused delegation or parallel work when it provides a clear benefit.
- Use LSP/code intelligence and repository search to validate assumptions and understand dependencies.
- After meaningful implementation work, check diagnostics and run appropriate tests, typechecks, lint, or builds.
- If verification fails, diagnose the root cause before making another change.
- Apply a focused fix, then re-run the failed verification.
- Continue the diagnose → fix → verify cycle until the failure is resolved or a genuine blocker requires user input.
- Before completion, review the final diff and verify that the original request and plan are fully satisfied.
- Do not declare completion with unexplained known failures.

Do not make speculative refactors or repeated edits without new evidence.

</system-reminder>
